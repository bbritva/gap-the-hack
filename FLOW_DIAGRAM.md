# System Flow Diagram

## Complete User Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                         TEACHER FLOW                                 │
└─────────────────────────────────────────────────────────────────────┘

    ┌──────────────┐
    │   Teacher    │
    │   Login      │
    └──────┬───────┘
           │
           ▼
    ┌──────────────┐
    │   Teacher    │
    │  Dashboard   │
    └──────┬───────┘
           │
           ▼
    ┌──────────────┐
    │   Create     │
    │   Session    │
    └──────┬───────┘
           │
           │ 1. Enter title
           │ 2. Upload PDF
           │ 3. Click submit
           │
           ▼
    ┌──────────────────────┐
    │  Mock Quiz Generator │
    │  (3 questions)       │
    └──────┬───────────────┘
           │
           ▼
    ┌──────────────────────┐
    │  Create Session API  │
    │  /api/sessions/create│
    └──────┬───────────────┘
           │
           │ - Generate 4-digit code
           │ - Save to database
           │ - Create questions
           │
           ▼
    ┌──────────────────────┐
    │   Session Page       │
    │   Display Code: 1234 │
    │   Show Questions     │
    │   Monitor Students   │
    └──────────────────────┘


┌─────────────────────────────────────────────────────────────────────┐
│                         STUDENT FLOW                                 │
└─────────────────────────────────────────────────────────────────────┘

    ┌──────────────┐
    │   Student    │
    │   (Phone)    │
    └──────┬───────┘
           │
           ▼
    ┌──────────────┐
    │  Join Page   │
    │  /student/   │
    │   join       │
    └──────┬───────┘
           │
           │ 1. Enter name
           │ 2. Enter code (1234)
           │ 3. Click join
           │
           ▼
    ┌──────────────────────┐
    │  Validate Code API   │
    │  /api/sessions/      │
    │  validate?code=1234  │
    └──────┬───────────────┘
           │
           │ ✓ Code valid
           │
           ▼
    ┌──────────────────────┐
    │  Interests Page      │
    │  (Optional)          │
    └──────┬───────────────┘
           │
           │ Select interests
           │ or skip
           │
           ▼
    ┌──────────────────────┐
    │  Join Session API    │
    │  /api/students/join  │
    └──────┬───────────────┘
           │
           │ - Create student record
           │ - Link to session
           │ - Return studentId
           │
           ▼
    ┌──────────────────────┐
    │  Session Waiting     │
    │  Page                │
    │  /student/session/1  │
    └──────┬───────────────┘
           │
           │ Display:
           │ - Welcome message
           │ - Session title
           │ - Waiting for teacher
           │
           ▼
    ┌──────────────────────┐
    │  Quiz Page           │
    │  (When started)      │
    │  /student/quiz/1     │
    └──────────────────────┘


┌─────────────────────────────────────────────────────────────────────┐
│                      DATABASE STRUCTURE                              │
└─────────────────────────────────────────────────────────────────────┘

    ┌─────────────────┐
    │    Teachers     │
    ├─────────────────┤
    │ id (PK)         │
    │ email           │
    │ name            │
    │ created_at      │
    └────────┬────────┘
             │
             │ 1:N
             │
    ┌────────▼────────┐
    │    Sessions     │
    ├─────────────────┤
    │ id (PK)         │
    │ teacher_id (FK) │
    │ title           │
    │ code (UNIQUE)   │◄────── "1234"
    │ status          │
    │ created_at      │
    └────┬───────┬────┘
         │       │
         │ 1:N   │ 1:N
         │       │
    ┌────▼───┐  ┌▼──────────┐
    │Questions│  │ Students  │
    ├─────────┤  ├───────────┤
    │id (PK)  │  │id (PK)    │
    │session_id│ │session_id │
    │question │  │name       │
    │options  │  │interests  │
    │correct  │  │joined_at  │
    │topic    │  └─────┬─────┘
    │difficulty│       │
    │points   │       │ 1:N
    └─────────┘       │
                 ┌────▼────────┐
                 │  Responses  │
                 ├─────────────┤
                 │ id (PK)     │
                 │ student_id  │
                 │ question_id │
                 │ answer      │
                 │ is_correct  │
                 │ time_taken  │
                 └─────────────┘


┌─────────────────────────────────────────────────────────────────────┐
│                      API ENDPOINTS                                   │
└─────────────────────────────────────────────────────────────────────┘

Teacher APIs:
├── POST   /api/sessions/create
│   └── Creates session with questions
│
├── GET    /api/sessions
│   └── Lists all teacher's sessions
│
├── GET    /api/sessions/[id]
│   └── Gets session details
│
├── POST   /api/sessions/[id]/end
│   └── Ends a session
│
└── GET    /api/sessions/[id]/stats
    └── Gets session analytics

Student APIs:
├── GET    /api/sessions/validate?code=1234
│   └── Validates session code
│
├── POST   /api/students/join
│   └── Joins student to session
│
├── GET    /api/questions?sessionId=1
│   └── Gets questions for session
│
├── POST   /api/responses
│   └── Submits student answer
│
└── GET    /api/leaderboard?sessionId=1
    └── Gets session leaderboard


┌─────────────────────────────────────────────────────────────────────┐
│                   MOCK QUIZ GENERATION                               │
└─────────────────────────────────────────────────────────────────────┘

    PDF Upload
        │
        ▼
    ┌─────────────────────────┐
    │  Mock Quiz Generator    │
    │  /lib/mock-quiz-        │
    │  generator.ts           │
    └────────┬────────────────┘
             │
             │ Generates 3 questions:
             │
             ├─► Question 1 (Foundation)
             │   "What is the main topic?"
             │   Points: 100
             │
             ├─► Question 2 (Application)
             │   "Which describes the concept?"
             │   Points: 100
             │
             └─► Question 3 (Analysis)
                 "How would you apply this?"
                 Points: 100


┌─────────────────────────────────────────────────────────────────────┐
│                   MOBILE ACCESS                                      │
└─────────────────────────────────────────────────────────────────────┘

    Student's Phone
         │
         │ Opens browser
         │
         ▼
    http://[server-ip]:3000/student/join
         │
         │ Responsive UI
         │ Touch-friendly
         │ Dark mode support
         │
         ▼
    ┌─────────────────┐
    │  Mobile View    │
    │  ┌───────────┐  │
    │  │   Name    │  │
    │  ├───────────┤  │
    │  │   Code    │  │
    │  ├───────────┤  │
    │  │   Join    │  │
    │  └───────────┘  │
    └─────────────────┘


┌─────────────────────────────────────────────────────────────────────┐
│                   DATA FLOW                                          │
└─────────────────────────────────────────────────────────────────────┘

Teacher Creates Session:
    Browser → API → Database → Response
    
    1. POST /api/sessions/create
       {
         title: "Biology Quiz",
         questions: [Q1, Q2, Q3]
       }
    
    2. Database INSERT
       - sessions table
       - questions table
    
    3. Response
       {
         sessionId: 1,
         code: "1234"
       }

Student Joins:
    Phone → API → Database → Response
    
    1. GET /api/sessions/validate?code=1234
       Response: { valid: true, sessionId: 1 }
    
    2. POST /api/students/join
       {
         name: "John",
         code: "1234",
         interests: ["sports"]
       }
    
    3. Database INSERT
       - students table
    
    4. Response
       {
         studentId: 1,
         sessionId: 1
       }
    
    5. Redirect to /student/session/1


┌─────────────────────────────────────────────────────────────────────┐
│                   SECURITY & VALIDATION                              │
└─────────────────────────────────────────────────────────────────────┘

Session Code:
├── 4 digits (0000-9999)
├── Unique in database
├── Generated randomly
└── Validated on join

Student Join:
├── Name required (non-empty)
├── Code must be 4 digits
├── Session must exist
├── Session must be active
└── Creates unique student record

Data Storage:
├── All data in Vercel Postgres
├── No mock data in production
├── Persistent across restarts
└── Supports concurrent users
```

## Key Features

### ✅ Implemented
- Mock quiz generation (3 questions)
- Session creation with unique codes
- Student join with code validation
- Mobile-responsive UI
- Database persistence
- Real-time validation
- Interest selection
- Session waiting page

### 🔄 Future Enhancements
- AI-powered PDF analysis
- Real-time WebSocket updates
- Quiz timer and scoring
- Live leaderboard
- Session analytics
- Question randomization
- Multi-language support
- Export results to CSV

## Technology Stack

- **Frontend:** Next.js 16, React 19, TailwindCSS
- **Backend:** Next.js API Routes
- **Database:** Vercel Postgres
- **Authentication:** NextAuth.js
- **Deployment:** Vercel
- **Language:** TypeScript
